#ifndef NEW_NODE_H  //if a compiler comes to this file multiple times it will show an error because it will try to define many times, so we use ifndef to make sure it wasnt defined before
#define NEW_NODE_H
/* ^^ these are the include guards */

/* Prototypes for the functions */
void new_node_before(int *value, int *criteria);

#endif
